Coloca aquí el archivo de video del trailer principal con el nombre `trailer.mp4`.
El HTML en `public/index.html` busca `assets/video/trailer.mp4`.

Si prefieres usar YouTube, reemplaza el elemento `<video>` en `public/index.html` por un iframe con la URL embebida.
